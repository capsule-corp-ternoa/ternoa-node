name: Start New Build Machine

on:
  workflow_call:

jobs:
  start_machine:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: "Run Script"
        run: ./.github/scripts/start-new-machine.sh